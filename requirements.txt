# AI Tax Research Assistant - Dependencies

# Core AI/LLM
anthropic>=0.25.0          # Claude API — sends questions, gets answers

# Vector database & embeddings
chromadb>=0.4.0            # Local vector database (stores our document chunks)
sentence-transformers>=2.7.0  # Converts text to embeddings (numerical meaning)

# PDF processing
pypdf>=4.0.0               # Extracts text from IRS PDF publications

# Web UI
streamlit>=1.35.0          # Turns Python into a web app with almost no code

# Utilities
python-dotenv>=1.0.0       # Loads your API key from the .env file
requests>=2.31.0           # Downloads files from the internet
